#!/usr/bin/env bash
# Strip Cursor co-author trailers injected by the IDE.
MSG_FILE="$1"
if [[ -f "${MSG_FILE}" ]]; then
  sed -i '' '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "${MSG_FILE}" 2>/dev/null \
    || sed -i '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "${MSG_FILE}"
fi
